name: Generate Galaxy Profile SVGs

on:
  schedule:
    - cron: "0 */12 * * *"
  workflow_dispatch:
  push:
    paths:
      - "galaxy-generator/config.yml"
      - "galaxy-generator/generator/**"

jobs:
  generate:
    name: Generate galaxy profile assets
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r galaxy-generator/requirements.txt

      - name: Generate SVGs
        working-directory: galaxy-generator
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: python -m generator.main

      - name: Commit and push if changed
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add galaxy-generator/assets/generated/
          if ! git diff --staged --quiet; then
            git commit -m "chore: update galaxy profile SVGs [skip ci]"
            git push
          fi
